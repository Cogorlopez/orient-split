<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="rikka.shizuku.permission.API_23" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_split_lr"
        android:allowBackup="true"
        android:supportsRtl="true">

        <!-- Setup / permission screen launched from the app icon -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault.DayNight">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Quick Settings tile -->
        <service
            android:name=".SplitTileService"
            android:exported="true"
            android:label="@string/tile_label"
            android:icon="@drawable/ic_split_lr"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Shizuku user service (runs in a privileged process via Shizuku) -->
        <service
            android:name=".ShizukuUserService"
            android:exported="false" />

        <!-- Shizuku provider (required for Shizuku API) -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:exported="true"
            android:multiprocess="false"
            android:enabled="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

    </application>

</manifest>
